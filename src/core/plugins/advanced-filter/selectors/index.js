import { getMatcherOption, getMatcherOptions, getMatcherOptionsAsComponents } from "./matcher-options"
import { getFilteredSpec } from "./filtered-spec"
import { getPhrase } from "./phrase"
import { isEnabled } from "./enabled"
import { getActiveMatchers, getMatcher, getMatchersAsComponents } from "./matcher"

export default {
  getMatcherOptionsAsComponents,
  getMatchersAsComponents,
  getMatcherOptions,
  getMatcherOption,
  getFilteredSpec,
  getActiveMatchers,
  getMatcher,
  getPhrase,
  isEnabled,
}
