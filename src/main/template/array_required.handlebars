
<fieldset>
    <legend>
        {{#unless isRoot}}<span class='document-name required'>{{name}}</span>{{/unless}}
        <span class='document-type'>array[ {{refDataType}} ]</span>
        <a class='array-remove' id="{{id}}-remove">delete item</a></div>
        <a class='array-add' id="{{id}}-add">add item</a>
    </legend>
    <div class='array-inner' id="{{id}}"></div>
    <input class='property' type='hidden' name="{{path}}." value="dummy"/>
</fieldset>
