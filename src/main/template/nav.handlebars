{{#each resourcesArray}}
	<li>
		<a class="nav-resource-title" href="#{{this.attributes.viewId}}">{{this.id}}</a>
		<ul>
		{{#each this.attributes.typeModels}}
			<li>
				<a class="nav-operation-description" href="#{{this.attributes.viewId}}">{{this.attributes.name}}</a>
			</li>
		{{/each}}
		</ul>
	</li>
{{/each}}
