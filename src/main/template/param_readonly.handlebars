<div class='code'>{{name}}</div>
<div>
    {{#if isBody}}
        <textarea class='body-textarea' readonly='readonly' name='{{name}}'>{{default}}</textarea>
    {{else}}
        {{#if default}}
            {{default}}
        {{else}}
            (empty)
        {{/if}}
    {{/if}}
</div>

