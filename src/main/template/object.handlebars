
<fieldset>
    <legend>
        {{#unless isRoot}}<span class='document-name'>{{name}}</span>{{/unless}}
        <span class='document-type'>{{dataType}}</span>
        <a class='object-undefined' id="{{id}}-undefined">remove object</a>
    </legend>
    <div class='object-inner' id="{{id}}"></div>
    <input class='property' type='hidden' name="{{path}}." value="dummy"/>
</fieldset>
